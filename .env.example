# Copy to apps/preview-engine/.env.local for local dev. Never commit a real .env.

# --- Preview generator (BRI-181) ---
# Required only when running `pnpm gen:preview` against real Claude Haiku 4.5.
# Without it, the CLI falls back to deterministic stub copy so the build still works.
ANTHROPIC_API_KEY=
