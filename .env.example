# Copy to apps/preview-engine/.env.local for local dev. Never commit a real .env.

# v0.0 has no required env vars yet. Supabase + outbound integrations
# will land in D2/D3 follow-up issues under [BRI-116].
