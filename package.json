{
  "name": "brilworks-experiments",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "dev": "pnpm --filter preview-engine dev",
    "gen:preview": "pnpm --filter preview-engine gen:preview"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}
